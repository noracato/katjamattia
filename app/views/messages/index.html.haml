.title messages

-# controller that fetches updates
.list-messages.col
  - @messages.each do |message|
    .col.message-container
      .row
        .name= message.name
        .time= message.created_at
      .row
        .message= message.message

.add-message.white-bg
  = simple_form_for :message, remote: true do |f|
    = f.input :name, input_html: { class: 'form-control-lg' }, grid_wrapper_html: { class: 'col-sm-6 '}
    = f.input :message, as: :text, input_html: { class: 'textbox' }, grid_wrapper_html: { class: 'col-sm-6 '}
    = f.button :submit, "Send", class: 'btn btn-success btn-lg'
